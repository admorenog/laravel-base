docker exec -it -u www-data web sh -c "$(curl -fsSL https://raw.github.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"
docker cp ~/.ssh web:/var/www/
docker cp ~/.gitconfig web:/var/www/

docker exec -it db bash
mysql -uroot -psecret
create database db

docker exec -it -u www-data web bash
composer install
php artisan migrate
php artisan db:seed
